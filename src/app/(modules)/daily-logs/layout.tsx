const DailyLogsLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <section>
      {/* Side Navigation Bar */}

      {children}
    </section>
  );
};

export default DailyLogsLayout;
